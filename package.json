{
  "name": "playwright-framework-v2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker:ui": "ENV=docker playwright test --grep @ui --project=desktop-chromium-authenticated --project=safari-authenticated",
    "docker:api": "ENV=docker playwright test --grep @api --project=api",
    "local:ui": "ENV=local playwright test --grep @ui --project=desktop-chromium-authenticated --project=safari-authenticated",
    "local:api": "ENV=local playwright test --grep @api --project=api",
    "cloud:ui": "ENV=cloud playwright test --grep @ui --project=desktop-chromium-authenticated --project=safari-authenticated",
    "cloud:api": "ENV=cloud playwright test --grep @api --project=api",
    "docker-compose:ui": "docker-compose up -d saucedemo petstore && docker-compose build playwright && ENV=docker TRACE=on docker-compose run --remove-orphans --no-deps playwright npm run docker:ui",
    "docker-compose:api": "docker-compose up -d saucedemo petstore && docker-compose build playwright && ENV=docker TRACE=on docker-compose run --remove-orphans --no-deps playwright npm run docker:api",
    "report:open": "playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.53.1",
    "@types/dotenv": "^8.2.3",
    "@types/node": "^24.0.4",
    "dotenv": "^16.5.0",
    "zod": "^4.1.5"
  }
}
