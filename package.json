{
  "name": "playwright-framework-v2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker:ui": "ENV=docker playwright test --grep @ui --project=docker-chromium",
    "docker:api": "ENV=docker playwright test --grep @api --project=api",

    "local:ui": "ENV=local playwright test --grep @ui --project=local-chromium",
    "local:api": "ENV=local playwright test --grep @api --project=api",

    "cloud:ui": "ENV=cloud playwright test --grep @ui --project=cloud-chromium",
    "cloud:api": "ENV=cloud playwright test --grep @api --project=api",

    "docker-compose:ui": "docker-compose up -d saucedemo petstore && ENV=docker docker-compose run --no-deps playwright npm run docker:ui",
    "docker-compose:api": "docker-compose up -d saucedemo petstore && ENV=docker docker-compose run --no-deps playwright npm run docker:api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.53.1",
    "@types/dotenv": "^8.2.3",
    "@types/node": "^24.0.4",
    "dotenv": "^16.5.0",
    "zod": "^4.1.5"
  }
}
